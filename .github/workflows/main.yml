name: CI
on: 
  push:

jobs:
  test:
    name: run tests
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
      - name: setup nodejs
        uses: actions/setup-node@v4
      - run: npm install
      - run: npm run typecheck
      - run: npm test
